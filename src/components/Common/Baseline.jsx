export const Baseline = ({ children }) => (
  <div
    style={{
      margin: "24px 0",
    }}>
    <hr
      style={{
        borderTop: "2px solid #646fd4",
      }}>
      {children}
    </hr>
  </div>
);
