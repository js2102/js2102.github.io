---
title: "유용한 Git 명령어 모음집"
thumbnail: "./git.png"
thumbnail_alt: "git"
summary: "파도파도 끝이 없는 Git"
initial_date: "2022-09-07"
modified_date: "2022-10-03"
deploy: false
keywords:
  - Git
toc:
  - 1. push
  - 2. cherry-pick
---

import { Image } from "../../../src/components/Common/Image";
import { Link } from "../../../src/components/Common/Link";
import { Source } from "../../../src/components/Common/Source";
import { Baseline } from "../../../src/components/Common/Baseline";
import { Highlight } from "../../../src/components/Common/Highlight";
import { Blockquote } from "../../../src/components/Common/Blockquote";
import { TextHighlight } from "../../../src/components/Common/TextHighlight";

<Blockquote>
  git을 사용하면서 알게된 유용한 Git 명령어들을 최대한 정리해보리고 했다.
</Blockquote>

## 1. push

### 1.1 HEAD보다 이전의 커밋까지만 push해야 하는 경우

```
git push <Remote명> <commit SHA:Branch명>
```

<br />

### 1.2 로컬에 있는 사항을 강제로 force push해야 하는 경우

```
git push <Remote명> <Branch명> -f
```
```
git push <Remote명> +<Branch명>
```

- 단, 강제로 force push 할 때, 다른 사용자들과 공유되는 상황이라면 커밋들이 유실될 수 있다.
- 따라서, 로컬 저장소와 비교했을 때, 원격 저장소에 새로운 커밋이 추가되어 있는 경우
- 덮어쓰기 작업을 취소시키는 --force-width-lease 옵션을 사용하는 것이 더 안전한다.

```
git push <Remote명> <Branch명> --force-width-lease
```

<br />
<br />

## 2. cherry-pick

```
git cherry-pick <commit SHA>
```